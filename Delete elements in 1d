#include<iostream.h>
#include<conio.h>
void main()
{
clrscr();
int a[20],n,i,del,count=0;
cout<<"enter element to be entered: ";
cin>>n;
cout<<"enter the values:\n";
for(i=0;i<n;i++)
{
cin>>a[i];
}
cout<<"\Nenter the value to be deketed:\n";
cin>>del;
for(i=0;i<n;i++)
{
if(a[i]==del)
{
for(int j=i;j<(n-1);j++)
{
a[j]=a[j+1];
}
count++;
n=n-1;
break;
}
}
if(count==0)
{
cout<<"element not found";
}
else
{
cout<<"element deleted succesfully\n";
cout<<"new array:\n";
for(i=0;i<n;i++)
{
cout<<a[i]<<"\n";
}
}
getch();
}
